# OpenMindBlocks/CMakeLists.txt
cmake_minimum_required(VERSION 3.21)
project(OpenMindBlocks)

# ... lo que ya tengas (find_package(Qt6 ...) para tu GUI) ...

add_subdirectory(launcher)  # ← añade el subproyecto
